# つづもん 公開前タスク（2026-07-31 → 08-05）

**これが公開までの残作業の正本。** 作成 2026-07-31（公開まで5日）。

先行するドキュメントは役割を分けて残す:

| ドキュメント | 役割 |
|---|---|
| `つづもん-公開告知プラン-20260728.md` | Instagram / LINE の**告知の中身**（文面・画像・カレンダー） |
| `.steering/20260724-tsudumon-flow-overhaul/launch-checklist.md` | LP・法務まわりの**監査結果**の記録 |
| `.steering/20260727-parent-handoff/tasklist.md` | 保護者導線の**実装タスク**の記録 |
| `つづもん-引き継ぎ-20260726.md` / `つづもん-機能ロードマップ.md` | 7/26 時点の棚卸し。**「未実装」表記は古い** |

> ⚠️ 「コードがある」と「本番で動く」は別。判断は必ず
> `firebase functions:list` と `gcloud functions describe <fn> --region=asia-northeast1
> --format="yaml(environmentVariables)"` の**実測**で行うこと。7/31 の調査では、
> コード・テスト・デプロイが全て揃っているのに**環境変数1本の欠落で機能がまるごと死んでいた**。

---

## 🔴 P0 — これが欠けたら公開しても売れない

### 1. `TSUDUMON_INVITE_SECRET` を本番へ反映する

**症状**: 秘密鍵が本番の環境変数に無く、`createTsudumonInvite()` が `null` を返していた
（`functions/src/tsudumonParent.ts:166-169`）。結果、**保護者導線が丸ごと無効**:

```
子が「おうちの人にわたすカード」を発行  ← ここで失敗（null）
  → QRコード                              ← 到達しない
  → 保護者が自分の端末で決済              ← 到達しない
  → 親子連携・きょうだい割引               ← 到達しない
  → 保護者ダッシュボード                   ← 到達しない
```

中学生本人は決済できない設計なので、**唯一の課金経路が塞がっていた**。

- [x] `functions/.env` に `TSUDUMON_INVITE_SECRET` を追加（2026-07-31・48バイト base64url）
- [ ] **保護者系5関数を名前指定でデプロイ**
  ```
  firebase deploy --only functions:tsudumonInviteCreate,functions:tsudumonInviteView,\
  functions:tsudumonInviteQr,functions:tsudumonParentCheckout,functions:tsudumonParentLink
  ```
- [ ] 反映確認
  ```
  gcloud functions describe tsudumonInviteCreate --region=asia-northeast1 \
    --project=chatstudy-63477 --format="yaml(environmentVariables)"
  ```
  に `TSUDUMON_INVITE_SECRET` が出ること

⚠️ **秘密鍵を後から変えると、既に配ったカードのURLが全て無効になる。** 公開前に確定させる。

### 2. 体験終了リマインドが実際に届くか、自分のLINEで1周テストする

告知で人を集めても、**体験が切れる日に何も届かなければそこで終わる**。
8/5 に始めた人の判断日は 8/8。コードの有無ではなく「**自分の端末に届いたか**」で確認する。

- [ ] 体験開始 → `tsudumonTrialReminder`（B-1〜B-3）が届く
- [ ] 「今日の1単元」（`tsudumonDailyUnit`）が設定時刻に届く
- [ ] B-1 の本文の案内先が実物と合っているか（リッチメニューは適用済みなので「メニューから」で可）

### 3. 本番Stripeで決済を1件通す

- [ ] Checkout → 決済完了 → **御礼push** → **全19単元が開く**
- [ ] Firestore の `users/{uid}` に `tsudumon.plan` と **`paidBy`** が入る
- [ ] 保護者ペイリンク経由でも同じことが起きる（P0-1 の反映後）

### 4. Stripe ダッシュボードで利用規約URLを設定する

`consent_collection[terms_of_service]='required'` を入れる前提の設定。
**未設定のまま required を入れると決済がエラーで止まる。**

- [ ] ダッシュボードに `https://tsudumon.jp/terms.html` を登録
- [ ] 登録後に `required` を入れるかを判断（入れなくても公開はできる）

### 5. marutto-study の未コミット分をコミットする

**本番にデプロイ済みのコードが git 履歴に無い**状態だった（151ファイル）。
壊れたときロールバックできない。

- [ ] コミット（テスト 1,334件 全通過を確認済み）

---

## 🟠 P1 — 8/5 当日までに

### 実装・設定

- [ ] **GA4 測定IDを入れる** — `lp/index.html` の `window.TZM_GA_ID = ''`
      計装（`tzmTrack()`）は完成済みで、**IDを1行入れるだけ**。
      空のままだと公開日の数字が一切取れない
- [ ] **保護者用リッチメニューを本番作成**
      `npx tsx scripts/setup-tsudumon-richmenu.ts --variant parent`
      → 出力IDを `.env` の `LINE_TSUDUMON_RICHMENU_PARENT` へ → `tsudumonParentLink` を再デプロイ
      （既定メニューにはしない。連携した保護者にだけ uid 単位でリンクする）
- [ ] `invoice.payment_failed` のテストイベント疎通
- [ ] 教材ゲートの実機確認 — 未ログイン / 体験中 / 体験切れ / 購入者 / 旧 `tzm-lic` 残存端末

### 告知オペレーション

| 期限 | やること |
|---|---|
| 8/1 | 一問一答LINE【1通目】おわび＋予告（10:00〜11:00 送信）。**下の「名前の扱い」を先に決める** |
| 8/1まで | 一問一答（`@824cebif`）の**8月の配信枠**を確認。7月は上限で停止していた |
| 8/2 | Instagram `0802/parent-price.png`（保護者向け）→ LPの保護者ページへリンク |
| 8/3 | Instagram 3回目（`0803/w-letter` → `w-read` → `w-stand`） |
| 8/5朝まで | **Instagramプロフィールのリンクを `tsudumon.jp` へ変更** |
| 8/5 | Instagram 当日（`0805/w-today` → `w-free`）。`w-free` にリンクスタンプ |
| 8/5 | **ハイライト「つづもん」を作成**（ストーリーは24時間で消える。常設導線はここ） |
| 8/5 | 一問一答LINE【2通目】公開（17:00〜18:00 送信） |
| 8/8 | 一問一答LINE【3通目】体験リマインド |

### ⚠️ 名前の扱いが食い違っている（要判断）

7/28 の決定は「**名前は 8/5 まで伏せる**」だった。しかし 7/30 に投稿した
`0730/news-2.png` が「**名前は、つづもん。**」と明かしている。

さらに 8/1 のLINE添付画像 `0801/line-0801.png` には「**まだ言えません。**」が焼き込まれており、
本文案も「まだ言えません」のまま。**Instagram と LINE の両方を見ている人にちぐはぐに映る。**

→ 8/1 の配信前に、本文と添付画像の両方を「名前は公開済み」前提に直すかを決める。

---

## 🟡 P2 — 公開後すぐ／余裕があれば

- [ ] **利用者の声セクション** — 実声3件そろうまでコメントアウト維持
      （例文を実在の声のように載せると景表法の優良誤認）
- [ ] **未成年者の同意チェック**を申込前に置くか判断（対象が中学生なので取消権リスクは現実的）
- [ ] **特商法の住所・電話番号**「請求があれば開示」運用 — Stripe審査で表示を求められる場合あり
- [ ] **保護者への週次レポート配信**（ロードマップ フェーズI・解約抑止に直結。
      前提の日別ログ `tsudumonProgress.days` は実装済みで、**配信だけ**が残っている）
- [ ] record版リッチメニューへの切替（登録者が増えてから。コマンド1行）
- [ ] `marutto-study/vercel.json` L52-58 の `/tsudumon/dl` rewrite の扱いを決める

---

## ✅ 2026-07-31 の実測で確認できたもの（再確認不要）

| 項目 | 結果 |
|---|---|
| 保護者系 Functions 8本 | ✅ 全てデプロイ済み（`InviteCreate/View/Qr` `ParentCheckout/Link/Dashboard/Portal/RenameChild`） |
| cron 4本 | ✅ `tsudumonDailyUnit` / `TrialReminder` / `Lifecycle` / `Recap` デプロイ済み |
| 販売ページ | ✅ 10ページすべて 200（LP・保護者3種・設定・アカウント・規約・特商法・プライバシー・マップ） |
| Stripe | ✅ **本番モード**（`sk_live`）。きょうだい価格 Price も設定ずみ |
| LP相談チャット | ✅ 本番で「月額1,280円」を正しく回答（きょうだい980円・解約も知識に入っている） |
| `www.tsudumon.jp` | ✅ Hosting 側で 301（パス・クエリ保持）。クライアント側ガードの追加は不要 |
| robots / sitemap | ✅ LP と保護者ページはインデックス可、会員ページは Disallow |
| 教材リンク健全性 | ✅ `deploy_tsudumon.py --check` エラー0（全19章） |
| Functions テスト | ✅ 1,334件 全通過 |
| 法務ページ | ✅ 特商法・利用規約16条・プライバシー（AI送信と外国移転まで）・保護者ページ |

---

## 触るときの注意（変わらない原則）

1. **一問一答（`lineWebhook` / 無料・3,000人）は無改修が原則。** つづもんの都合で触らない
2. デプロイは**必ず名前指定**（`firebase deploy --only functions:xxx`）
3. `functions/.env` の**値は読まない・出力しない**
4. リッチメニューの本実行・cronのアーム・hostingデプロイは**ユーザー承認後**
5. テストは `npx --prefix functions vitest run --root functions`
6. **常時無料なのは1節「律令国家と奈良時代」**。「1単元ぜんぶ無料」と書かない
